import { SummarizerClient } from "./summarizer-client";

/**
 * Summarizer Configuration Page
 *
 * Server component that renders the summarizer configuration UI.
 * Handles AI model configuration, metric selection, and monitoring.
 */
export default function SummarizerPage() {
  return <SummarizerClient />;
}
